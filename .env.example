NODE_ENV=development
PORT=5000

# Database - Use DATABASE_URL for Railway, or individual PG variables for local development
# For Railway: use the DATABASE_URL provided by the PostgreSQL plugin
DATABASE_URL=

# For local development, use standard PostgreSQL environment variables:
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=anycomp_db

# JWT (Optional)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:3000